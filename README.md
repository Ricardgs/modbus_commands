After cloning the project, extract the files inside Project_Configuration_Linux.zip into the project root directory.

Also, run the command:
  git submodule update --init --recursive
The command is necessary in order to run the python script that writes/reads registers via modbus
